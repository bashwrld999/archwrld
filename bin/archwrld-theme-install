#!/bin/bash

# archwrld-theme-install: Install a new theme from a git repo for ArchWRLD
# Usage: archwrld-theme-install <git-repo-url>

if [ -z "$1" ]; then
  echo -e "\e[32mSee https://manuals.omamix.org/2/the-archwrld-manual/90/extra-themes\n\e[0m"
  REPO_URL=$(gum input --placeholder="Git repo URL for theme" --header="")
else
  REPO_URL="$1"
fi

if [ -z "$REPO_URL" ]; then
  exit 1
fi

THEMES_DIR="$HOME/.config/archwrld/themes"
THEME_NAME=$(basename "$REPO_URL" .git | sed -E 's/^archwrld-//; s/-theme$//')
THEME_PATH="$THEMES_DIR/$THEME_NAME"

# Remove existing theme if present
if [ -d "$THEME_PATH" ]; then
  rm -rf "$THEME_PATH"
fi

# Clone the repo directly to ~/.config/archwrld/themes
if ! git clone "$REPO_URL" "$THEME_PATH"; then
  echo "Error: Failed to clone theme repo."
  exit 1
fi

# Apply the new theme with archwrld-theme-set
archwrld-theme-set $THEME_NAME
